apiVersion: v1
kind: Service
metadata:
  name: fitness-tracker-service
  namespace: fitness
spec:
  type: LoadBalancer
  selector:
    app: fitness-tracker-app
  ports:
  - port: 80
    targetPort: 5000
